import React from "react";
import ReactDOM from "react-dom/client";
import PacksApp from "./PacksApp.jsx";
import "./packs.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PacksApp />
  </React.StrictMode>
);
